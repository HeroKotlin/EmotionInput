<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="@dimen/emotion_input_toolbar_height"
                android:orientation="horizontal"
                android:background="@drawable/emotion_input_toolbar">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/iconList"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:layout_marginTop="@dimen/emotion_input_toolbar_top_border_width"/>

    <Button
        android:id="@+id/submitButton"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/emotion_input_toolbar_top_border_width"
        android:text="@string/emotion_input_submit_button_title"
        android:textSize="@dimen/emotion_input_submit_button_text_size"
        android:elevation="20dp"
        android:textAllCaps="false"
        android:minWidth="0dp"
        android:minHeight="0dp"
        style="?android:attr/borderlessButtonStyle"/>

</LinearLayout>